# Changelog — [Client Name] Brand Kit

<!--
  Version history for the strategy/ files (positioning.md, message-house.md) and the
  brand/ files (brand-context.md, brand-voice.md, brand-visual.md, retired-language.md),
  plus reference/evidence.md.

  This file lives OUTSIDE the brand files so that project chats never pay its context cost.
  It is read in exactly two situations:
    1. A human reviewing kit history on GitHub.
    2. The brand-sync skill deduplicating calls it has already mined (the ledger below).

  Keep entries compact: version, date, sources, one line per section touched. The full
  narrative rationale for a change belongs in the pull request description, not here.
-->

## Mined calls ledger

<!--
  Consumed by brand-sync when deciding which Fathom calls have already been mined into a
  released version. Append one row per call, per release. Append-only — never delete rows,
  even after the related content is superseded.
-->

| Call date | Fathom link | Mined into |
|---|---|---|
| YYYY-MM-DD | https://fathom.video/calls/... | v1.1 |

## Entries

<!--
  Newest first. Format per entry:

  ### vX.Y (YYYY-MM-DD)
  Sources: [call links or "internal" / "client email"]
  - §N [section name] — [one line: what changed]
  - live-holds — [holds added / lifted]
-->

### v1.0 (YYYY-MM-DD)
Initial kit created from brand-kit-template.
